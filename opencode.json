{
  "$schema": "https://opencode.ai/config.json",
  "skills": {
    "paths": [".opencode/skills"]
  },
  "agent": {
    "spec-agent": {
      "mode": "subagent",
      "description": "Analyze digital IP requirements and produce user-approved specs."
    },
    "rtl-agent": {
      "mode": "subagent",
      "description": "Implement RTL based only on approved specs."
    },
    "verify-agent": {
      "mode": "subagent",
      "description": "Build verification plans, tests, assertions, and reports."
    }
  }
}
